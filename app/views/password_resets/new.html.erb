<h1>Maybe try noting your password down somewhere next time eh?</h1>

<div class="row">
  <div class="col-xs-6 col-xs-offset-3">
    <%= form_for(:password_reset, url: password_resets_url) do |f| %>
      <%= f.label :email  %>
      <%= f.email_field :email, class: "form-control" %>

      <br>

      <%= f.submit "Reset my password", class: "btn btn-primary" %>

    <% end %>
  </div>
</div>
